[pytest]
# Make every async test function run under asyncio automatically.
asyncio_mode = auto

# Add backend/ to sys.path so "from services.xxx import ..." works in tests.
pythonpath = .

# Only look for tests here.
testpaths = tests
